# =============================================================================
# PRODUCTION server block
# Listens on port 80 (exposed as host port 9400 in docker-compose)
# Serves /marva from /prod-quartz
# =============================================================================
server {
    listen       80;
    listen  [::]:80;
    server_name  localhost;

    #charset koi8-r;
    #access_log  /var/log/nginx/host.access.log  main;

    location / {
        #root   /usr/share/nginx/html;
        #index  index.html index.htm;
        return 301 $scheme://$host/marva/;
    }

    # Profile editor redirect ------

    # the server listens to which region in the refferes
    location /ldp/verso/configs {
        rewrite ^/ldp/verso/configs/(.*)$ /util/profiles/$1;
    }


    location /profile-editor-stage/server/whichrt {
        rewrite ^/profile-editor-stage/server/whichrt$ /util/whichrt;
    }

    location /profile-editor-prod/server/whichrt {
        rewrite ^/profile-editor-stage/server/whichrt$ /util/whichrt;
    }

    location /profile-editor-stage {
      rewrite ^/profile-editor-stage$ /profile-editor-stage/ last;
      alias /profile-editor;
      index  index.html index.htm;
    }

    location /profile-editor-prod {
      rewrite ^/profile-editor-prod$ /profile-editor-prod/ last;
      alias /profile-editor;
      index  index.html index.htm;
    }


    # the server listens to which region in the refferes
    location /api/listconfigs {
        rewrite ^/api/listconfigs$ /util/profiles/$arg_where;
    }

    location /listconfigs {
        rewrite ^/listconfigs$ /util/profiles/$arg_where;
    }


    # doesn't matter if the assets come from the stage or prod
    location ~ ^/(assets|node_modules)/ {
            rewrite  ^/(assets|node_modules)/(.*)$  /profile-editor-stage/$1/$2;
    }



    # Marva editor ------
    location /marva {
      rewrite ^/marva$ /marva/ last;
      alias /prod-quartz;
      try_files $uri $uri/ /marva/index.html;
    }

    # Deprecated: redirect old paths to /marva/
    location /editor {
      return 301 /marva/;
    }

    location /quartz {
      return 301 /marva/;
    }


    # Services under /marva/ prefix ------
    location /marva/api-production/ {
        proxy_pass http://util:5200/api-production/;
    }

    location /marva/util/ {
        # big timeout here because util can deploy, which can take some time
        proxy_read_timeout 3600;
        proxy_connect_timeout 3600;
        proxy_send_timeout 3600;
        send_timeout 3600;
        proxy_pass http://util:5200/; # note the trailing slash!
    }

    location /marva/dancer/ {
        proxy_read_timeout 3600;
        proxy_connect_timeout 3600;
        proxy_send_timeout 3600;
        send_timeout 3600;
        proxy_pass http://dctap-dancer:3000/; # note the trailing slash!
    }

    location /marva/scriptshifter/ {
        proxy_redirect off;
        proxy_set_header host $host;
        proxy_set_header X-real-ip $remote_addr;
        proxy_set_header X-forward-for $proxy_add_x_forwarded_for;

        proxy_pass http://scriptshifter:8000/; # note the trailing slash!
    }



}


# =============================================================================
# STAGING server block
# Listens on port 8080 (exposed as host port 9401 in docker-compose)
# Serves /marva from /prod-quartz
# =============================================================================
server {
    listen       8080;
    listen  [::]:8080;
    server_name  localhost;

    #charset koi8-r;
    #access_log  /var/log/nginx/host.access.log  main;

    location / {
        #root   /usr/share/nginx/html;
        #index  index.html index.htm;
        return 301 $scheme://$host/marva/;
    }

    # Profile editor redirect ------

    # the server listens to which region in the refferes
    location /ldp/verso/configs {
        rewrite ^/ldp/verso/configs/(.*)$ /util/profiles/$1;
    }


    location /profile-editor-stage/server/whichrt {
        rewrite ^/profile-editor-stage/server/whichrt$ /util/whichrt;
    }

    location /profile-editor-prod/server/whichrt {
        rewrite ^/profile-editor-stage/server/whichrt$ /util/whichrt;
    }

    location /profile-editor-stage {
      rewrite ^/profile-editor-stage$ /profile-editor-stage/ last;
      alias /profile-editor;
      index  index.html index.htm;
    }

    location /profile-editor-prod {
      rewrite ^/profile-editor-prod$ /profile-editor-prod/ last;
      alias /profile-editor;
      index  index.html index.htm;
    }


    # the server listens to which region in the refferes
    location /api/listconfigs {
        rewrite ^/api/listconfigs$ /util/profiles/$arg_where;
    }

    location /listconfigs {
        rewrite ^/listconfigs$ /util/profiles/$arg_where;
    }

    # doesn't matter if the assets come from the stage or prod
    location ~ ^/(assets|node_modules)/ {
            rewrite  ^/(assets|node_modules)/(.*)$  /profile-editor-stage/$1/$2;
    }


    # Marva editor ------
    location /marva {
      rewrite ^/marva$ /marva/ last;
      alias /prod-quartz;
      try_files $uri $uri/ /marva/index.html;
    }

    # Deprecated: redirect old paths to /marva/
    location /editor {
      return 301 /marva/;
    }

    location /quartz {
      return 301 /marva/;
    }

    # Services under /marva/ prefix ------
    location /marva/api-staging/ {
        if ($request_method = 'OPTIONS') {
          add_header 'Access-Control-Allow-Origin' '*';
          add_header 'Access-Control-Allow-Credentials' 'true';
          add_header 'Access-Control-Allow-Headers' 'Authorization,Accept,Origin,DNT,X-CustomHeader,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Content-Range,Range';
          add_header 'Access-Control-Allow-Methods' 'GET,POST,OPTIONS,PUT,DELETE,PATCH';
          add_header 'Access-Control-Max-Age' 1728000;
          add_header 'Content-Type' 'text/plain charset=UTF-8';
          add_header 'Content-Length' 0;
          return 204;
        }

        proxy_redirect off;
        proxy_set_header host $host;
        proxy_set_header X-real-ip $remote_addr;
        proxy_set_header X-forward-for $proxy_add_x_forwarded_for;

        proxy_pass http://util:5200/api-staging/;
    }

    location /marva/util/ {
        if ($request_method = 'OPTIONS') {
          add_header 'Access-Control-Allow-Origin' '*';
          add_header 'Access-Control-Allow-Credentials' 'true';
          add_header 'Access-Control-Allow-Headers' 'Authorization,Accept,Origin,DNT,X-CustomHeader,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Content-Range,Range';
          add_header 'Access-Control-Allow-Methods' 'GET,POST,OPTIONS,PUT,DELETE,PATCH';
          add_header 'Access-Control-Max-Age' 1728000;
          add_header 'Content-Type' 'text/plain charset=UTF-8';
          add_header 'Content-Length' 0;
          return 204;
        }

        proxy_redirect off;
        proxy_set_header host $host;
        proxy_set_header X-real-ip $remote_addr;
        proxy_set_header X-forward-for $proxy_add_x_forwarded_for;

        # big timeout here because util can deploy, which can take some time
        proxy_read_timeout 3600;
        proxy_connect_timeout 3600;
        proxy_send_timeout 3600;
        send_timeout 3600;
        proxy_pass http://util:5200/; # note the trailing slash!
    }

    location /marva/dancer/ {
        if ($request_method = 'OPTIONS') {
          add_header 'Access-Control-Allow-Origin' '*';
          add_header 'Access-Control-Allow-Credentials' 'true';
          add_header 'Access-Control-Allow-Headers' 'Authorization,Accept,Origin,DNT,X-CustomHeader,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Content-Range,Range';
          add_header 'Access-Control-Allow-Methods' 'GET,POST,OPTIONS,PUT,DELETE,PATCH';
          add_header 'Access-Control-Max-Age' 1728000;
          add_header 'Content-Type' 'text/plain charset=UTF-8';
          add_header 'Content-Length' 0;
          return 204;
        }

        proxy_redirect off;
        proxy_set_header host $host;
        proxy_set_header X-real-ip $remote_addr;
        proxy_set_header X-forward-for $proxy_add_x_forwarded_for;

        proxy_read_timeout 3600;
        proxy_connect_timeout 3600;
        proxy_send_timeout 3600;
        send_timeout 3600;
        proxy_pass http://dctap-dancer:3000/; # note the trailing slash!
    }

    location /marva/scriptshifter/ {
        proxy_redirect off;
        proxy_set_header host $host;
        proxy_set_header X-real-ip $remote_addr;
        proxy_set_header X-forward-for $proxy_add_x_forwarded_for;

        proxy_pass http://scriptshifter:8000/; # note the trailing slash!
    }






}